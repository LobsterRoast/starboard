[package]
name = "starboard"
version = "0.1.0"
edition = "2024"

[dependencies]
evdev = { version = "0.13.2", features = ["tokio", "stream-trait"] }
tokio = { version = "1.47.1", features = ["full"] }
